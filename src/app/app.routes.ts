import { Routes } from '@angular/router';
import { TasksComponent } from './pages/tasks/tasks-page.component';

export const routes: Routes = [
    {
        path: '',
        component: TasksComponent,
    },
];
